# Flux-managed. See clusters/eye-of-michael/flux-system/storage.yaml for the
# Kustomization CR that reconciles this path. Third cutover under #15, after
# observability and database.
#
# democratic-csi/ (see its README, #13) isn't listed here yet - it needs
# Talos's iscsi-tools extension on every node and the democratic-csi-truenas
# Secret created first, or the HelmRelease just fails to reconcile. Add
# democratic-csi/namespace.yaml, helmrepository.yaml and helmrelease.yaml
# here once both are done.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - nfs-provisioner.yaml
