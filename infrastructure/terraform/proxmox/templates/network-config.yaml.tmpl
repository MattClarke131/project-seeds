machine:
  network:
    hostname: ${hostname}
    interfaces:
      - interface: eth0
        addresses:
          - ${ip_address}/24
        routes:
          - network: 0.0.0.0/0
            gateway: 10.0.10.1
