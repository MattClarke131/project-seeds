apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: radarr-standard
  namespace: downloads-standard
spec:
  ingressClassName: nginx
  rules:
  - host: radarr.labmatt.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: radarr-standard
            port:
              number: 7878
