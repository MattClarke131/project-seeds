apiVersion: v1
kind: Secret
metadata:
  name: bazarr-secret
  namespace: downloads-standard
# deployment.yaml's initContainer pins this into Bazarr's config.yaml on
# every boot. Regenerating the API key in Bazarr's UI gets clobbered back
# to this value on next restart.
data: {}
