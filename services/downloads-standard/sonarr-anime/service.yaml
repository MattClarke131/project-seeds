apiVersion: v1
kind: Service
metadata:
  name: sonarr-anime
  namespace: downloads-standard
spec:
  type: ClusterIP
  selector:
    app: sonarr-anime
  ports:
  - port: 8989
    targetPort: 8989
    protocol: TCP
    name: http
