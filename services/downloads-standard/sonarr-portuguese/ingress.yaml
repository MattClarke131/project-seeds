apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: sonarr-portuguese
  namespace: downloads-standard
spec:
  ingressClassName: nginx
  rules:
  - host: sonarr-portuguese.labmatt.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: sonarr-portuguese
            port:
              number: 8989
